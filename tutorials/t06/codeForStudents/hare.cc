export module hare;
import racer;

export
class Hare /* fill in inheritance declaration */ {
    // Fill in any needed data fields that aren't inherited.

  public:
    Hare(); // Can be removed if not needed.
    void tick();
};
