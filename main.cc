#include "Player.h"
#include "Subject.h"
#include "GameState.h"
#include "Observer.h"


int main(int argc, char** argv) {
    // Initializing Players and GameState
    int x = 0;
    bool graphics = false;
    while(x < argc){
        // 
        string arg = argv[x];
        if(arg == "-graphics" ){
            graphics = true;
        }else if(arg == ""){
            iji
        }

        x++;
    }

    if(graphics){
        GraphicObserver* = new(graphic)
    }else{
        
    }
}